# 全站后台管理页面 — 规划文档

> 文档版本：v1.2（2026-08-06）
> 目标：为「青鸾传世」网站规划一套**图片 + 文本**内容后台管理系统，并支持客户留资收集与导出 Excel。
> 部署背景：当前站点处于**开发测试阶段**，暂用 GitHub Pages / 本地静态托管；**后续正式部署到服务器**。本规划以**服务器部署**为主要目标，同时兼容开发测试阶段的运行方式。
> 说明：当前页面为纯静态 HTML，图片均为 **CSS 渐变占位**（无真实图片文件），文本直接内嵌于 HTML。本规划用于后续在「网页页面搭建完毕」后，指导后台开发。

---

## 一、现状盘点（后台需要管理的资源）

站点包含 13 个页面，需要管理的资源分两大部分：**图片** 与 **文本**。

### 1.1 图片占位清单（全部待替换为真实图片）

| 位置 | 占位类/容器 | 数量 | 说明 |
|---|---|---|---|
| 首页 · 轮播 | `.slide-content`（轮播背景） | 4 | 主视觉大图 |
| 首页 · 项目主视觉 | `.projects-hero .box` | 1 | 精选项目大图 |
| 首页 · 精选项目 | `.project-thumb.thumb-1 ~ thumb-6` | 6 | 项目缩略图 |
| 首页 · 最新消息 | `.news-thumb.news-1/2/3` | 3 | 新闻封面 |
| 团队页 | `.member-avatar.b1 ~ b6`（含 `modal-avatar`） | 6 | 成员头像 |
| 故事页 | `.story-hero-image`、`.story-mid-image`、`.story-image-box` | 3 | 故事配图 |
| 承诺页 | `.commitment-hero-image`、`.feature-image` | 2+ | 承诺配图 |
| 项目页 | `.projects-hero`、`.projects-piece` | 4 | 项目主视觉/分类 |
| 方法页 | `.hero-image-caption` 等 | 1+ | 方法论配图 |

### 1.2 文本内容清单（全部待可编辑）

| 位置 | 字段 | 示例 |
|---|---|---|
| 各页面横幅 | 标题 `<h1>`、副标题 `<p class="sub">`、英文标签 `section-label` | 欢迎语 / 认识我们的团队 |
| 首页 · 轮播 | 4 组大标题 + 副标题 | 「国际设计工作室」 |
| 首页 · 各区块 | `studio/projects/commitment/news` 的标题、正文、链接文字 | 「我们的方法论」「了解更多→」 |
| 首页 · 项目卡 | 项目名 `.name` + 分类 `.cat` | 「综合商业综合体 / Commercial」 |
| 首页 · 新闻卡 | 日期 `.date` + 标题 + 链接 | 「2024.05.12」 |
| 团队页 · 成员 | 姓名 + 职位 + 简介（`data-name/role/bio`） | XXX / Founder / 简介 |
| 团队页 · 简介区 | 「About Us」段落 | 团队背景介绍 |
| 子页面 | 各正文段落、feature 文本、分类名称 | 方法论、剪纸/珠宝/服装/游戏分类 |
| 页脚 | 导航链接、社交链接、版权文字 | © 2024 青鸾传世 |

---

## 二、后台整体目标

1. **不写代码也可以更新网站内容**——运营人员通过后台修改文本、上传图片，自动生成站点所需内容。
2. **图片统一管理**——一处上传，全站占位替换。
3. **兼容两种部署阶段**——开发测试阶段（GitHub Pages / 本地静态托管）与正式生产阶段（**服务器部署**）均可运行；正式以**服务器 + 后端 + 数据库**为实现基准，开发阶段可用轻量方式预览验证。
4. **客户留资收集**——前台提供「联系我们/留下您的需求」入口，收集客户联系方式，统一沉淀到后台并支持导出 Excel（生产环境基于服务器存储）。

---

## 三、技术方案建议（供后续开发参考）

### 3.1 关键判断：为什么必须有后端

由于网站**后续要部署到服务器**，且需要「客户留资收集 + 导出 Excel」这类动态数据功能，**方案应包含后端与数据库**，而不只是纯静态托管。后端是承载登录校验、图片上传、内容存库、客户留资存储与导出的基础。

### 3.2 推荐技术栈（以服务器部署为主）

| 类别 | 推荐选项 | 说明 |
|---|---|---|
| **后端** | Node.js（Express/Nest）或 Python（Flask/Django） | 提供 REST API，处理登录、内容管理、图片上传、客户留资 |
| **数据库** | MySQL / PostgreSQL / SQLite（轻量） | 存储图片记录、页面内容、成员、客户留资 |
| **后台前端** | Vue / React（或独立后台模板） | 后台管理界面，对接后端 API |
| **图片存储** | 服务器目录 或 对象存储（如阿里云 OSS / 腾讯云 COS） | 真实图片上传与访问 |
| **部署** | 云服务器（Nginx + 进程守护 PM2 / systemd）或云托管 | 生产环境运行 |

> 这也天然解决了客户留资问题：表单直接 POST 到**自己的后端**，写入数据库，无需第三方表单服务，权限可控、可导出 Excel。

### 3.3 开发测试阶段（当前）的适配

- 正式部署前，可在**本地**起后端服务（Node/Flask）+ 数据库跑通后台功能，用 **localhost/局域网**验证。
- 前台页面目前托管于 **GitHub Pages / 本地静态服务器**用于纯展示测试。
- 跨阶段注意：**静态页与后台 API 分离**。前台页通过 JS 请求后台接口（配置 API 地址），这样：
  - 开发测试时，前台连本地/内网 API；
  - 生产时，前台连服务器 API，无需改动前台代码结构，只改一处 API 域名配置。
- 图片占位在当前测试阶段可继续用 CSS 渐变；接入后台后替换为 `site_images` 中的真实图片。

### 3.4 方案对比（满足需求时的权衡）

| 方案 | 后端 | 客户留资 | 部署 | 适用 |
|---|---|---|---|---|
| **A. 独立后端 + 数据库（推荐）** | 有 | ✅ 存库+导出 | 服务器 | 正式生产、数据可控 |
| **B. 无头 CMS + 静态生成** | 有（CMS） | 视服务而定 | 静态托管/服务器 | 内容多偏静态 |
| **C. 纯静态 + 云函数** | 无 | 依赖 Serverless | 静态托管 | 轻量、低成本、当前测试够用 |

> 最终选型待网页搭建完毕、确认服务器环境后敲定。当前规划按「字段 + 模块」中立描述，不绑定具体框架。

### 3.5 双端适配原则（移动端 + 电脑端）

网站最终需要**移动端与电脑端（桌面）双端正常浏览与操作**。规划遵循以下原则：
- **前台页面**：采用响应式布局（CSS 媒体查询断点：手机 <768px、平板 768–1024px、桌面 >1024px），同一套代码自适应两端的导航、轮播、网格、表单。
- **后台管理**：同样做双端适配——桌面以「左侧菜单 + 右侧工作区」呈现；移动端收起为「顶部菜单 / 可折叠抽屉」，保证手机也能进行内容编辑、查看客户留资、导出等操作。
- **交互差异**：桌面支持鼠标悬停、拖拽排序；移动端改为点击、上下滑动排序。
- **图片**：一张原图，后台自动生成 mobile / tablet / desktop 多尺寸（对应各页占位），两端加载合适尺寸以提速。
- 当前前台已经具备基础响应式（media query），将其作为双端实现起始点，后台开发沿用同一响应式规范。

---

## 四、后台功能模块

### 4.0 登录与权限
- 管理员账号登录（用户名 + 密码 / OAuth）。
- 分角色：**管理员**（全部权限）、**内容编辑**（仅编辑文本）、**媒体管理员**（仅图片）。
- 操作审计日志（谁改了什么、什么时候）。

### 4.1 首页内容管理
- **轮播管理**：4 组轮播的（大标题 / 副标题 / 背景图 / 链接）增删改、排序、启用停用。
- **项目主视觉**：上传精选大图 + 说明文字。
- **精选项目管理**：6 个项目卡 = 图片 + 项目名 + 分类 + 排序。
- **新闻管理**：新闻卡 = 封面图 + 日期 + 标题 + 跳转链接，支持增删。
- **方法论/承诺/故事区块**：标题 + 正文 + 「了解更多」链接文字与目标链接。

### 4.2 子页面管理
- 每个子页面（故事/项目/方法论/承诺/剪纸/珠宝/服装/游戏/新闻/团队）的：
  - **横幅**：标题、副标题、英文标签。
  - **正文**：各段落标题 + 内容（富文本或纯文本）。
  - **配图**：页内各图片占位上传。
  - **列表项**：如分类、feature 项，支持增删排序。

### 4.3 团队成员管理
- 成员列表：姓名、职位、简介、头像、排序。
- 每条成员 = `name / role / bio / avatar`（对应现 `.member-card` 的 `data-*` + `member-avatar`）。
- 支持新增/删除/排序成员，数量不限于 6。

### 4.4 图片资源库管理
- 统一图片库（上传 / 批量上传 / 预览 / 删除 / 重命名）。
- 图片可打标签、按规格筛选（如「成员头像 1:1」「横幅宽图」）。
- **替换流程**：选择图片 → 关联到某个站点图片占位 → 前台自动生效。
- 自动生成多尺寸（缩略图/原图）以适配不同占比占位。

### 4.5 页面/内容通用编辑
- 每个页面一个「内容表单」，对应字段可视化编辑。
- 富文本编辑（加粗、列表、链接）用于正文段落。
- 所有字段均带**输入校验**（必填、长度、链接格式）。

### 4.6 客户留资管理（联系方式收集）
负责前台「联系我们 / 留下您的需求」表单提交的客户信息沉淀、查看与导出。

**前台入口**
- 全站页脚 / 首页「联系」区块 / 「合作咨询」处放置留言表单。
- 表单字段（可配置）：姓名、公司/机构、邮箱、电话、城市、留言内容、意向类型（项目咨询/合作/求职等）、来源页面。
- 提交后前端校验 + 国家/电话号码格式校验 + 简单验证码（防垃圾提交）。

**后台管理**
- **客户留资列表**：分页展示所有提交记录，支持搜索（姓名/邮箱/电话/关键词）、按日期/意向类型/来源页面筛选。
- **详情查看**：单条留资的完整字段 + 提交时间 + 来源页面 + IP（可选）。
- **状态管理**：标记「新线索 / 跟进中 / 已联系 / 已关闭」，记录跟进备注与下次联系时间。
- **通知**：新留资实时/邮件通知管理员。
- **删除与垃圾过滤**：过滤重复、垃圾、无效联系方式。

---

## 五、后台数据模型（核心字段）

### 5.1 站点图片表 `site_images`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 唯一标识 |
| key | string | 占位标识，如 `home.slide_1`、`member.avatar_1` |
| title | string | 图片标题 |
| url | string | 图片文件路径 |
| alt | string | 替代文本（利于 SEO/无障碍） |
| thumbnail_url | string | 缩略图 |
| sort | int | 排序 |
| status | boolean | 是否启用 |
| updated_at | datetime | 更新时间 |

### 5.2 文本内容表 `page_contents`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 唯一标识 |
| page | string | 所属页面（如 `index`、`team-members`） |
| section | string | 区块标识（如 `hero`、`projects`、`news`） |
| field | string | 字段名（如 `title`、`subtitle`、`body`） |
| content | text | 文本内容 |
| lang | string | 语言版本（zh/en，若需多语言） |
| updated_at | datetime | 更新时间 |

### 5.3 成员表 `team_members`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int | 主键 |
| name / role / bio | string / string / text | 姓名 / 职位 / 简介 |
| avatar | string | 头像图片路径 |
| sort | int | 显示排序 |
| status | boolean | 是否展示 |

### 5.4 客户留资信息表 `customer_leads`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int | 主键 |
| name | string | 姓名 |
| company | string | 公司/机构 |
| email | string | 邮箱（用于联系回访） |
| phone | string | 电话（用于联系回访） |
| city | string | 城市 |
| intent_type | string | 意向类型（项目咨询/合作/求职等） |
| message | text | 留言内容 |
| source_page | string | 来源页面 |
| ip_address | string | 来源 IP（可选，用于防垃圾） |
| status | string | 线索状态（新线索/跟进中/已联系/已关闭） |
| note | text | 跟进备注 |
| next_contact_at | datetime | 下次联系时间 |
| created_at | datetime | 提交时间 |
| updated_at | datetime | 更新时间 |

---

## 六、后台界面布局规划

```
┌────────────────────────────────────────────────┐
│ 顶部导航：Logo | 首页 | 团队 | 图片库 | 页面 | 预览 | 退出 │
├──────────────┬─────────────────────────────────┤
│ 左侧菜单      │  内容工作区                       │
│ ┌──────────┐ │  ┌ Dashboard 概览 (资源统计)  ┐  │
│ │ 控制台    │ │  └──────────────────────────┘  │
│ │ 首页内容  │ │  ┌ 表单区                    ┐  │
│ │ 页面管理  │ │  │ 标题 [____]               │  │
│ │ 团队成员  │ │  │ 正文 [富文本]             │  │
│ │ 客户联系  │ │  │ 图片 [上传/选择]          │  │
│ │ 图片库    │ │  │ 【保存】【取消】           │  │
│ │ 新闻动态  │ │  └──────────────────────────┘  │
│ │ 设置      │ │          ▼ 客户联系子菜单       │
│ └──────────┘ │   留资列表 │ 导出Excel │ 状态筛选  │
├──────────────┴─────────────────────────────────┤
│ 底部状态栏：最后保存时间 / 待发布草稿数            │
└────────────────────────────────────────────────┘
```

- **左侧菜单**：按栏目导航。
- **工作区**：编辑表单，右侧或下方可实时「预览」。
- **图片选择器**：从图片库选图，替换对应占位。

---

## 七、操作流程

### 7.1 编辑文本
1. 登录后台 → 左侧选「页面管理」→ 选页面/区块。
2. 在表单中修改标题、正文。
3. 点「保存」→ 生成/更新对应静态内容。
4. 点「预览」查看效果 → 「发布」部署上线。

### 7.2 上传/替换图片
1. 进入「图片库」→ 上传新图（或选已有图）。
2. 在「页面管理」中定位到目标图片占位 → 点「选择图片」。
3. 确认尺寸/裁切 → 保存 → 前台占位被替换。

### 7.3 发布
- **生产（服务器部署）**：「发布」将最新内容保存到数据库，前端页面/后台自动生效（动态渲染或构建推送）。
- **开发测试（GitHub Pages / 本地）**：通过后台导出的静态文件或本地构建预览验证。
- 支持「草稿 / 已发布」状态，避免误改上线。

### 7.4 客户留资收集与导出 Excel
**留资收集（前台）**
1. 访客在「联系我们 / 合作咨询」表单填写姓名、邮箱、电话等并提交。
2. 表单提交到后端/表单服务 → 存储到 `customer_leads` 表。
3. 后台即时收到新线索通知。

**查看与管理（后台）**
1. 登录后台 → 左侧「客户联系」→ 留资列表。
2. 查看详情、标记联系状态（新线索/跟进中/已联系/已关闭）、记录跟进备注。
3. 用搜索/筛选快速定位（按日期、意向类型、来源页面、状态）。

**导出 Excel（后台）**
1. 在「留资列表」选择导出范围：**全部 / 当前筛选结果 / 勾选指定记录**。
2. 选择导出字段（姓名、公司、邮箱、电话、城市、意向、留言、来源页、状态、提交时间等）。
3. 点「导出 Excel」→ 后台生成 **.xlsx / .csv** 文件 → 浏览器下载。
4. 导出兼容 Excel 打开，表头为中文字段，支持用于后续客户跟进。

**技术实现（导出）**
- **前端导出**：用 SheetJS (xlsx) 在浏览器直接生成 .xlsx，适合数据量较小。
- **后端导出**（方案 C）：后端查询生成 Excel（如 Node `exceljs` / Python `openpyxl`），支持大数据量、并发与权限控制。
- 建议**导出权限仅限管理员**，防止客户隐私泄露。

---

## 八、双端适配规划（移动端 + 电脑端）

后台与前台均需 **移动端与电脑端双端可用**，规划要点：

### 8.1 前台页面（访客浏览）
- 响应式断点：手机 <768px、平板 768–1024px、桌面 >1024px。
- 各组件两端适配：导航（收起/展开菜单）、轮播（触控滑动/箭头）、项目与成员网格（单列/双列/三列）、新闻列表、客户留资表单（大按钮、自适应宽度）。
- 图片响应式：后台为同一张图自动输出 mobile / tablet / desktop 尺寸，前端按设备加载。

### 8.2 后台管理（运营操作）
- **桌面端**：左侧固定菜单 + 右侧工作区，侧边栏导航、分栏表单、表格展示、拖拽排序。
- **移动端**：菜单折叠为抽屉/顶部汉堡按钮；表格改为卡片式列表；表单单列布局；按钮与输入框加大、可触控。
- **关键操作两端可用**：文本编辑、图片上传/替换、客户留资查看与状态更新、导出 Excel。
- **测试要求**：发布前分别在 Android / iOS 常见屏幕与主流桌面浏览器上验证功能与排版。

---

## 九、安全考量（服务器被攻击的防护）

网站部署到公网服务器后，面临被扫描、注入、暴力破解、DDoS 等威胁。规划以下安全措施：

### 9.1 传输与访问安全
- **全站 HTTPS**（SSL 证书），强制跳转，避免明文传输客户信息。
- **Web 应用防火墙（WAF）**：云厂商 WAF 或 nginx 规则，拦截常见攻击（SQL 注入 / XSS / 路径遍历）。
- 后台入口**独立域名或路径 + 访问白名单（IP 限制）**，弱化被扫描面。

### 9.2 登录与身份认证
- **强密码策略** + 多次失败锁定 / 限速，防暴力破解。
- 支持**两步验证（2FA / 手机令牌）**。
- 会话固定保护、合理过期、登出清除；登录失败与异地登录告警。

### 9.3 应用层防护（防注入 / XSS / 上传）
- 后端对**所有输入做参数化查询/转义**，防 SQL 注入与 XSS。
- **文件上传安全**：校验文件类型/大小，图片重命名，禁止上传脚本；目录不可执行。
- 富文本内容做白名单过滤（去脚本/事件属性），防止存储型 XSS。
- 接口接口限速/限频，防刷接口与恶意提交。

### 9.4 数据安全（含客户留资）
- **敏感字段加密/脱敏**：客户邮箱、电话在数据库中加密存，展示时部分脱敏。
- **数据库备份**：定时全量+增量备份，异地/对象存储留存，支持恢复演练。
- 导出 Excel 需密码/权限校验，且做脱敏；导出行为记录审计日志。
- 遵循隐私合规（如获客需告知、数据最小化、可删除）。

### 9.5 服务器与运维加固
- **系统与软件及时更新补丁**，关闭不必要端口与服务。
- 安全组/防火墙最小化开放（仅 80/443 等）。
- 进程以**低权限用户**运行；数据库与后端分离、不对外直接暴露。
- 日志监控 + 入侵检测（fail2ban 等）+ 异常流量告警。
- **DDoS 防护**（云厂商高防 / CDN），限流与访问控制。
- 定期安全扫描与依赖漏洞检查（如 npm audit）。

### 9.6 应急与恢复
- 备份可快速恢复；准备应急预案（被入侵时隔离、查杀、回滚）。
- 关键操多云审计，配合取证。

---

## 十、依赖关系与里程碑（后续实施顺序）

> 依赖：**网页页面搭建完毕**（本规划文档最终落地前的前置条件）+ **服务器环境确认**（部署目标）。

**开发测试阶段（当前 → 网页页面搭建完毕）**
1. **阶段 0（并行）**：确定后台技术栈（后端框架 / 数据库）；设计前台页面与后台 API 的**接口约定**（页面预留数据读取位、图片占位标识），确保后续可无缝接入后台。

**生产实施阶段（服务器部署，后台正式搭建）**
1. **阶段 1**：搭建服务器环境（Nginx + 后端进程 + 数据库），建立后端框架与数据库表结构。
2. **阶段 2**：实现登录鉴权 + 后台框架 + 侧边栏 + Dashboard。
3. **阶段 3**：实现图片库（上传/标识/替换），对接各页占位为真实图片。
4. **阶段 4**：实现文本内容表单（首页 → 子页面 → 团队 → 新闻）。
5. **阶段 5**：实现前台「联系我们/合作咨询」留资表单 + 后台客户留资列表 + 跟进状态管理（数据存服务器数据库）。
6. **阶段 6**：实现客户信息**导出 Excel**（字段选择 + .xlsx/.csv 下载），并做权限控制。
7. **阶段 7**：实现预览 + 发布流程（动态渲染上线；可选静态构建以兼容开发期托管）。
8. **阶段 8**：安全加固 + 双端适配完善 + 多语言（如需）+ 性能优化与备份（安全落实详见「九、安全考量」，「双端」详见「八、双端适配」）。

> 说明：阶段 1 依赖**服务器环境**；若开发阶段用本地后端先期验证，阶段 1 可在服务器就绪前以本地方式启动部分功能。

---

## 十一、备注（后续可扩展）
- 多语言（简体/繁体/英文）字段版本控制。
- 图片 CDN / 压缩，提升加载速度。
- SEO 管理（meta 标题、描述、关键词）纳入后台。
- 内容版本历史与回滚。
- 客户留资：邮件/短信自动提醒；与 CRM 对接；去重与数据合规（隐私保护，导出需脱敏处理）。
- **服务器运维相关**（生产部署时）：HTTPS 证书、域名绑定、数据库定时备份、后端日志与错误监控、访问限流、进程守护与自动重启、灰度/回滚。
- **数据可迁移**：数据库结构设计与前台页面「占位标识」保持稳定，便于从开发测试环境平滑迁移到生产服务器。

---

## 十二、要点回顾（部署到服务器的关键结论）

1. 网站**正式目标是服务器部署**，后台采用「**后端 + 数据库**」方案，而非纯静态。
2. 开发测试阶段（GitHub Pages / 本地）仅作展示验证；后台与前台**接口分离**，可平滑过渡。
3. **客户留资 + 导出 Excel** 在服务器架构下可独立、安全实现（数据落库、权限控制、可导出）。
4. **双端支持（移动端 + 电脑端）**：前台与后台均做响应式适配，同一套代码两端可用。
5. **安全优先**：全站 HTTPS、强认证、防注入/XSS/上传攻击、数据加密脱敏、备份与 WAF/DDoS 防护，并具备应急恢复能力——规划已覆盖服务器被攻击的常见场景。
6. 待网页页面搭建完毕并确认服务器环境后，再按第十节里程碑正式开发后台。

---

*本文档为规划稿，具体字段与实现细节将随网页搭建进度修正。*

---

## 十三、上传公网方式（测试用）

> 目的：把本地 `lead8-clone` 的最新站点内容同步推送至 GitHub 仓库并部署 GitHub Pages，从而获得公网访问链接。
> 适用于**开发测试阶段**；当站点上线正式服务器后，本节不再需要。

### 定位
- **源目录**：`C:\Users\Ye\Documents\GitHub\OttoDIYLib\lead8-clone`（开发目录，含 `.git` 与否均可）
- **部署仓库（本地克隆）**：`C:\Users\Ye\Documents\GitHub\QLCS_web`（真正的 git 仓库，含 `.git`，remote 指向 GitHub）
- **远程仓库**：`https://github.com/wz19921208/QLCS_web.git`（GitHub Pages 由 `main` 分支根目录构建）
- **公网链接**：`https://wz19921208.github.io/QLCS_web/`

### Git 客户端路径（本机可用但不默认在 PATH）
```
C:\Users\Ye\.workbuddy\vendor\PortableGit\cmd\git.exe
```

### 完整上传步骤

#### 第 1 步：将源目录内容镜像同步到部署仓库
```powershell
robocopy "C:\Users\Ye\Documents\GitHub\OttoDIYLib\lead8-clone" `
         "C:\Users\Ye\Documents\GitHub\QLCS_web" /MIR /XD .git /NJH /NJS /NDL /NFL /NC /NS
```
- `/MIR` 镜像模式：覆盖更新文件，并**删除**部署仓库中源目录已不存在的文件；
- `/XD .git`：排除部署仓库的 `.git` 目录，避免破坏 git 元数据。

#### 第 2 步：提交变更
```powershell
$git="C:\Users\Ye\.workbuddy\vendor\PortableGit\cmd\git.exe"
$q="C:\Users\Ye\Documents\GitHub\QLCS_web"
& $git -C $q add -A
& $git -C $q commit -m "<本次改动说明>"
```

#### 第 3 步：推送到远程
```powershell
& $git -C $q push origin main
```
- 成功标志：输出形如 `8c59053..deb84b8  main -> main`；
- 注明：push 进度信息会写入 stderr，PowerShell 可能提示 "Command failed"，但只要出现 `main -> main` 即**推送成功**。

#### 第 4 步：确认远程同步
```powershell
& $git -C $q ls-remote origin main
# 返回的 commit 应与本地 git log -1 --oneline 的 commit 一致
```

### 验证公网链接（GitHub Pages 自动部署，约 1-2 分钟生效）
```powershell
$base="https://wz19921208.github.io/QLCS_web/"
Invoke-WebRequest -Uri $base -UseBasicParsing -TimeoutSec 20   # 期望 200
Invoke-WebRequest -Uri ($base+"paper-personal.html") -UseBasicParsing # 抽查子页
```

### 关键要点
- 部署仓库 `QLCS_web` 必须先是一个 git 仓库（含 `.git`），否则无法 commit / push；
- 首次若在纯新机器上，需先在 GitHub 建同仓库并 `git clone` 到本地，再执行上述同步；
- 推送需具备该仓库的写权限与已配置的凭据（credential.helper 为 `manager`，走 Windows 凭据管理器）；
- 改动的源是 `lead8-clone`，**以它为唯一真源**，用 robocopy 单向镜像到部署仓库，避免在部署仓库直接编辑造成混乱。
